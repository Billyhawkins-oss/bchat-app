{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "rootDirectory": "backend",
    "startCommand": "npm install && npm run validate-env && npm start",
    "healthcheckPath": "/api/health"
  }
}